# Matrix Lab — Full-RGNB Research Pipeline (Demo)

A single end-to-end quant research pipeline — raw OHLCV → feature engineering →
event / triple-barrier labeling → leakage-free purged CV (CPCV) → model training
(RF / XGB / LGB / Logit / MLP) → walk-forward → backtest → risk → ensemble →
deployment export — rendered to static HTML.

**Synthetic / sample data only. No live data, no credentials, no real trained models.**
Methodology is real; displayed numbers are illustrative.

> Status: scaffolding — build in progress.
